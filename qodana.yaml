version: 1.0
failThreshold: 64
profile:
  name: qodana.recommended
include:
  - name: VulnerableLibrariesGlobal
bootstrap: |+
  cd Epsilon.Host.Frontend && pnpm install
dotnet:
  solution: Epsilon.sln
  configuration: Release
linter: jetbrains/qodana-dotnet:2023.2-eap